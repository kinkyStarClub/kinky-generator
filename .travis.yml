language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-kinky' ]; then cd .. && eval "mv $currentfolder generator-kinky" && cd generator-kinky; fi

